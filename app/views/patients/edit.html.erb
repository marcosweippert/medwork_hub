<div class="page-head">
  <div>
    <h1 class="page-title"><%= t("helpers.heading.patients.edit") %></h1>
    <p class="page-sub"><%= @patient.name %></p>
  </div>
  <div class="page-actions">
    <%= link_to t("ui.back"), patient_path(@patient), class: "btn btn--ghost" %>
  </div>
</div>
<section class="card form-card">
  <%= render "form", patient: @patient %>
</section>
