<div class="page-head">
  <div>
    <h1 class="page-title"><%= t("users.new") %></h1>
    <p class="page-sub"><%= t("users.new_subtitle") %></p>
  </div>
  <div class="page-actions">
    <%= link_to t("ui.back"), users_path, class: "btn btn--ghost" %>
  </div>
</div>
<section class="card form-card">
  <%= render "form", user: @user %>
</section>
