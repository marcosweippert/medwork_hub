<div class="page-head">
  <div>
    <h1 class="page-title"><%= t("patients.new") %></h1>
    <p class="page-sub"><%= t("patients.subtitle") %></p>
  </div>
  <div class="page-actions">
    <%= link_to t("ui.back"), patients_path, class: "btn btn--ghost" %>
  </div>
</div>
<section class="card form-card">
  <%= render "form", patient: @patient %>
</section>
