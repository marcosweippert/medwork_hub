<% if resource.errors.any? %>
  <div class="alert alert-danger">
    <strong><%= pluralize(resource.errors.count, "error") %> prohibited this record from being saved</strong>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
