<div class="page-head">
  <div>
    <h1 class="page-title">Edit room</h1>
    <p class="page-sub"><%= @room.name %></p>
  </div>
  <div class="page-actions">
    <%= link_to "Back", room_path(@room), class: "btn btn--ghost" %>
  </div>
</div>
<section class="card form-card--wide">
  <%= render "form", room: @room %>
</section>
